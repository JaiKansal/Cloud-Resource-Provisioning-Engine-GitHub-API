stages:
  - test

test:
  stage: test
  image: python:3.9
  script:
    - pip install -r requirements.txt
    - pytest -m smoke
  artifacts:
    expire_in: 1 week
    paths:
      - report.html
      - logs/
